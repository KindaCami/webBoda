<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        body, html {
            width: 100%; height: 100%;
            overflow: hidden; background: #000;
            font-family: 'Inter', sans-serif;
        }

        #container-3d {
            position: fixed; top: 0; left: 0;
            width: 100%; height: 100%; z-index: 1;
        }

        /* ── OVERLAY FADE (lo gestiona JS) ── */
        #fade-overlay {
            position: fixed; inset: 0;
            background: #000; opacity: 0;
            pointer-events: none; z-index: 5;
            transition: opacity 1.5s ease;
        }

        /* ── CONTENEDOR LOGIN ── */
        .login-overlay {
            position: fixed; inset: 0;
            display: flex; flex-direction: column;
            justify-content: center; align-items: center;
            z-index: 10;
            opacity: 0; pointer-events: none;
            transition: opacity 2s ease-in-out;
            padding: 20px;
        }

        /* ── LÍNEA SUPERIOR DE ESTADO ── */
        .status-bar {
            width: 100%; max-width: 460px;
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 16px;
            font-family: 'Orbitron', monospace;
            font-size: 0.65rem; letter-spacing: 0.15em;
            color: rgba(255,255,255,0.35);
        }
        .status-dot {
            width: 6px; height: 6px; border-radius: 50%;
            background: #00ff88; box-shadow: 0 0 8px #00ff88;
            display: inline-block; margin-right: 6px;
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.3; }
        }

        /* ── CARD PRINCIPAL ── */
        .card {
            width: 100%; max-width: 460px;
            background: rgba(8, 8, 20, 0.85);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 4px;
            overflow: hidden;
            box-shadow:
                0 0 0 1px rgba(0,255,136,0.05),
                0 30px 80px rgba(0,0,0,0.8),
                inset 0 1px 0 rgba(255,255,255,0.05);
        }

        /* Barra de color superior estilo Phaser */
        .card-accent {
            height: 3px;
            background: linear-gradient(90deg, #00ff88, #0088ff, #ff00cc);
        }

        .card-body { padding: 44px 40px 40px; }

        /* ── TÍTULO ── */
        .brand {
            font-family: 'Orbitron', monospace;
            font-size: 0.7rem; font-weight: 700;
            letter-spacing: 0.3em; text-transform: uppercase;
            color: #00ff88;
            margin-bottom: 24px;
        }

        h1 {
            font-family: 'Orbitron', monospace;
            font-size: 2.1rem; font-weight: 900;
            line-height: 1.1;
            color: #fff;
            margin-bottom: 8px;
        }
        h1 span { color: #00ff88; }

        .subtitle {
            font-size: 0.875rem; color: rgba(255,255,255,0.45);
            margin-bottom: 36px; font-weight: 300; letter-spacing: 0.03em;
        }

        /* ── LABEL + INPUT ── */
        .field { margin-bottom: 12px; }

        label {
            display: block;
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.2em;
            color: rgba(255,255,255,0.4);
            margin-bottom: 8px;
            text-transform: uppercase;
        }

        input[type="text"] {
            width: 100%;
            background: rgba(255, 255, 255, 0.04);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 4px;
            padding: 14px 16px;
            color: #fff;
            font-family: 'Orbitron', monospace;
            font-size: 1rem;
            letter-spacing: 0.25em;
            text-transform: uppercase;
            outline: none;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        input[type="text"]::placeholder {
            color: rgba(255,255,255,0.2);
            letter-spacing: 0.15em;
        }
        input[type="text"]:focus {
            border-color: rgba(0, 255, 136, 0.5);
            box-shadow: 0 0 0 3px rgba(0, 255, 136, 0.08);
            background: rgba(0, 255, 136, 0.03);
        }

        /* ── BOTÓN ── */
        button[type="submit"] {
            width: 100%; margin-top: 20px;
            padding: 15px 24px;
            background: #00ff88;
            color: #000;
            font-family: 'Orbitron', monospace;
            font-size: 0.8rem; font-weight: 700;
            letter-spacing: 0.2em; text-transform: uppercase;
            border: none; border-radius: 4px;
            cursor: pointer;
            transition: background 0.2s, box-shadow 0.2s, transform 0.1s;
            box-shadow: 0 0 20px rgba(0, 255, 136, 0.25);
        }
        button[type="submit"]:hover {
            background: #33ffaa;
            box-shadow: 0 0 30px rgba(0, 255, 136, 0.45);
        }
        button[type="submit"]:active { transform: scale(0.98); }

        /* ── ERROR ── */
        .error-msg {
            display: flex; align-items: center; gap: 10px;
            background: rgba(255, 60, 60, 0.08);
            border: 1px solid rgba(255, 60, 60, 0.25);
            border-left: 3px solid #ff3c3c;
            border-radius: 4px;
            padding: 12px 14px;
            margin-bottom: 20px;
            color: #ff7070;
            font-size: 0.82rem;
        }
        .error-icon { font-size: 1rem; flex-shrink: 0; }

        /* ── PIE ── */
        .card-footer {
            padding: 16px 40px;
            border-top: 1px solid rgba(255,255,255,0.05);
            font-size: 0.72rem;
            color: rgba(255,255,255,0.2);
            text-align: center;
            letter-spacing: 0.08em;
        }

        /* ── DECORACIÓN ESQUINAS ── */
        .corner {
            position: absolute;
            width: 12px; height: 12px;
            border-color: rgba(0,255,136,0.4);
            border-style: solid;
        }
        .corner-tl { top: -1px; left: -1px; border-width: 2px 0 0 2px; }
        .corner-tr { top: -1px; right: -1px; border-width: 2px 2px 0 0; }
        .corner-bl { bottom: -1px; left: -1px; border-width: 0 0 2px 2px; }
        .corner-br { bottom: -1px; right: -1px; border-width: 0 2px 2px 0; }

        .card-wrapper {
            position: relative;
            width: 100%; max-width: 460px;
        }

        /* ── MENSAJE DE BIENVENIDA ── */
        #welcome-msg {
            position: fixed;
            bottom: 60px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 6;
            text-align: center;
            pointer-events: none;
            opacity: 0;
            transition: opacity 1.5s ease;
            width: 90%;
            max-width: 560px;
        }
        #welcome-msg.visible { opacity: 1; }
        #welcome-msg.hidden  { opacity: 0; }

        .welcome-inner {
            background: rgba(8, 8, 20, 0.75);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-top: 2px solid rgba(0, 255, 136, 0.4);
            border-radius: 4px;
            padding: 24px 32px;
        }
        .welcome-tag {
            font-family: 'Orbitron', monospace;
            font-size: 0.55rem; letter-spacing: 0.3em;
            text-transform: uppercase; color: #00ff88;
            margin-bottom: 12px; display: block;
        }
        .welcome-text {
            font-family: 'Orbitron', monospace;
            font-size: clamp(0.9rem, 2.5vw, 1.15rem);
            font-weight: 700; color: #fff;
            line-height: 1.5; letter-spacing: 0.05em;
        }
        .welcome-text em {
            color: #00ff88; font-style: normal;
        }
        .welcome-sub {
            font-family: 'Inter', sans-serif;
            font-size: 0.8rem; color: rgba(255,255,255,0.35);
            margin-top: 10px; font-weight: 300; font-style: italic;
        }
        .welcome-dots {
            display: flex; justify-content: center; gap: 6px; margin-top: 14px;
        }
        .welcome-dot {
            width: 5px; height: 5px; border-radius: 50%;
            background: rgba(0,255,136,0.4);
            animation: dotPulse 1.5s infinite;
        }
        .welcome-dot:nth-child(2) { animation-delay: 0.3s; }
        .welcome-dot:nth-child(3) { animation-delay: 0.6s; }
        @keyframes dotPulse {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.3); background: #00ff88; }
        }
    </style>
</head>
<body>

    <div id="container-3d"></div>
    <div id="fade-overlay"></div>

    <div id="welcome-msg">
    <div class="welcome-inner">
        <span class="welcome-tag">✦ Mensaje de los novios</span>
        <div class="welcome-text">
            Abróchate los cinturones,<br>
            <em>gracias por acompañarnos</em><br>
            en este viaje tan importante para nosotros
        </div>
        <p class="welcome-sub">Explora el espacio mientras preparamos el acceso...</p>
        <div class="welcome-dots">
            <div class="welcome-dot"></div>
            <div class="welcome-dot"></div>
            <div class="welcome-dot"></div>
        </div>
    </div>
</div>

    <div id="login-form-container" class="login-overlay">

        <div class="status-bar">
            <span><span class="status-dot"></span>SISTEMA ACTIVO</span>
            <span>SECTOR NUPCIAL // 42</span>
        </div>

        <div class="card-wrapper">
            <!-- Esquinas decorativas -->
            <div class="corner corner-tl"></div>
            <div class="corner corner-tr"></div>
            <div class="corner corner-bl"></div>
            <div class="corner corner-br"></div>

            <div class="card">
                <div class="card-accent"></div>
                <div class="card-body">

                    <div class="brand">✦ Acceso de invitados</div>
                    <h1>Nuestra<br><span>Boda</span></h1>
                    <p class="subtitle">Introduce tu código para aterrizar</p>

                    <% if (error) { %>
                    <div class="error-msg">
                        <span class="error-icon">⚠</span>
                        <span><%= error %></span>
                    </div>
                    <% } %>

                    <form action="/login" method="POST">
                        <div class="field">
                            <label for="access_code">Código de acceso</label>
                            <input
                                type="text"
                                id="access_code"
                                name="access_code"
                                placeholder="••••••••"
                                autocomplete="off"
                                required
                            >
                        </div>
                        <button type="submit">Aterrizar →</button>
                    </form>
                </div>

                <div class="card-footer">
                    ¿Perdiste tu código? Contacta con los novios
                </div>
            </div>
        </div>

    </div>

    <script type="importmap">
      {
        "imports": {
          "three": "https://unpkg.com/three@0.160.0/build/three.module.js",
          "three/addons/": "https://unpkg.com/three@0.160.0/examples/jsm/"
        }
      }
    </script>
    <script type="module" src="/js/intro3d.js"></script>
</body>
</html>