<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuestra Boda · 2026 / 2027</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;600;700;900&family=Crimson+Pro:ital,wght@0,300;0,400;1,300;1,400&display=swap" rel="stylesheet">
    <style>
        :root {
            --green: #00ff88;
            --blue: #0088ff;
            --pink: #ff00cc;
            --gold: #ffd700;
            --bg: #000;
            --surface: rgba(8, 8, 20, 0.9);
            --border: rgba(255,255,255,0.07);
            --text-dim: rgba(255,255,255,0.4);
        }

        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        html { scroll-behavior: smooth; }

        body {
            background: var(--bg);
            color: #fff;
            font-family: 'Crimson Pro', Georgia, serif;
            overflow-x: hidden;
            cursor: none;
        }

        /* ── CURSOR PERSONALIZADO ── */
        .cursor {
            position: fixed;
            width: 8px; height: 8px;
            background: var(--green);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            transition: transform 0.1s;
            box-shadow: 0 0 12px var(--green);
        }
        .cursor-ring {
            position: fixed;
            width: 32px; height: 32px;
            border: 1px solid rgba(0,255,136,0.4);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9998;
            transition: transform 0.15s, width 0.2s, height 0.2s;
        }

        /* ── CANVAS DE ESTRELLAS (fijo detrás de todo) ── */
        #stars-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        /* ── NAV ── */
        nav {
            position: fixed; top: 0; left: 0; right: 0;
            z-index: 100;
            padding: 20px 48px;
            display: flex; justify-content: space-between; align-items: center;
            background: linear-gradient(to bottom, rgba(0,0,0,0.8), transparent);
            backdrop-filter: blur(0px);
            transition: background 0.3s, backdrop-filter 0.3s;
        }
        nav.scrolled {
            background: rgba(0,0,0,0.9);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
        }
        .nav-brand {
            font-family: 'Orbitron', monospace;
            font-size: 0.7rem; font-weight: 700;
            letter-spacing: 0.3em;
            color: var(--green);
            text-decoration: none;
        }
        .nav-links {
            display: flex; gap: 32px; list-style: none;
        }
        .nav-links a {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.2em;
            color: rgba(255,255,255,0.5);
            text-decoration: none;
            text-transform: uppercase;
            transition: color 0.2s;
        }
        .nav-links a:hover { color: var(--green); }
        .nav-cta {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; font-weight: 700;
            letter-spacing: 0.2em; text-transform: uppercase;
            color: #000;
            background: var(--green);
            padding: 10px 20px;
            border-radius: 3px;
            text-decoration: none;
            transition: background 0.2s, box-shadow 0.2s;
            box-shadow: 0 0 20px rgba(0,255,136,0.2);
        }
        .nav-cta:hover {
            background: #33ffaa;
            box-shadow: 0 0 30px rgba(0,255,136,0.4);
        }

        /* ── SECCIONES GENERALES ── */
        section {
            position: relative;
            z-index: 1;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* ── HERO ── */
        #hero {
            flex-direction: column;
            text-align: center;
            padding: 120px 24px 80px;
        }

        .hero-eyebrow {
            font-family: 'Orbitron', monospace;
            font-size: 0.65rem; letter-spacing: 0.35em;
            color: var(--green); text-transform: uppercase;
            margin-bottom: 28px;
            opacity: 0;
            animation: fadeUp 1s ease 0.3s forwards;
        }

        .hero-title {
            font-family: 'Orbitron', monospace;
            font-size: clamp(3rem, 8vw, 8rem);
            font-weight: 900;
            line-height: 0.95;
            letter-spacing: -0.02em;
            margin-bottom: 12px;
            opacity: 0;
            animation: fadeUp 1s ease 0.5s forwards;
        }
        .hero-title .accent { color: var(--green); }
        .hero-title .amp {
            color: rgba(255,255,255,0.15);
            font-weight: 400;
            font-family: 'Crimson Pro', serif;
            font-style: italic;
            font-size: 0.7em;
        }

        .hero-names {
            font-family: 'Crimson Pro', serif;
            font-style: italic;
            font-size: clamp(1.5rem, 4vw, 3rem);
            font-weight: 300;
            color: rgba(255,255,255,0.6);
            margin-bottom: 48px;
            opacity: 0;
            animation: fadeUp 1s ease 0.7s forwards;
        }

        /* Countdown */
        .countdown {
            display: flex; gap: 32px; justify-content: center;
            margin-bottom: 64px;
            opacity: 0;
            animation: fadeUp 1s ease 0.9s forwards;
        }
        .count-item { text-align: center; }
        .count-number {
            font-family: 'Orbitron', monospace;
            font-size: clamp(2rem, 5vw, 4rem);
            font-weight: 900;
            color: #fff;
            line-height: 1;
            display: block;
        }
        .count-label {
            font-family: 'Orbitron', monospace;
            font-size: 0.55rem; letter-spacing: 0.2em;
            color: var(--text-dim);
            text-transform: uppercase;
            display: block;
            margin-top: 6px;
        }
        .count-sep {
            font-family: 'Orbitron', monospace;
            font-size: 2rem;
            color: rgba(0,255,136,0.3);
            align-self: flex-start;
            padding-top: 8px;
        }

        .hero-cta-group {
            display: flex; gap: 16px; justify-content: center;
            flex-wrap: wrap;
            opacity: 0;
            animation: fadeUp 1s ease 1.1s forwards;
        }
        .btn-primary {
            font-family: 'Orbitron', monospace;
            font-size: 0.75rem; font-weight: 700;
            letter-spacing: 0.2em; text-transform: uppercase;
            color: #000; background: var(--green);
            padding: 16px 36px; border-radius: 3px;
            text-decoration: none;
            box-shadow: 0 0 30px rgba(0,255,136,0.3);
            transition: background 0.2s, box-shadow 0.2s, transform 0.1s;
        }
        .btn-primary:hover {
            background: #33ffaa;
            box-shadow: 0 0 50px rgba(0,255,136,0.5);
            transform: translateY(-2px);
        }
        .btn-secondary {
            font-family: 'Orbitron', monospace;
            font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase;
            color: rgba(255,255,255,0.6);
            padding: 16px 36px; border-radius: 3px;
            text-decoration: none;
            border: 1px solid rgba(255,255,255,0.12);
            transition: color 0.2s, border-color 0.2s;
        }
        .btn-secondary:hover {
            color: #fff;
            border-color: rgba(255,255,255,0.3);
        }

        /* Scroll indicator */
        .scroll-hint {
            position: absolute; bottom: 40px; left: 50%; transform: translateX(-50%);
            display: flex; flex-direction: column; align-items: center; gap: 8px;
            opacity: 0;
            animation: fadeIn 1s ease 1.5s forwards;
        }
        .scroll-hint span {
            font-family: 'Orbitron', monospace;
            font-size: 0.55rem; letter-spacing: 0.25em;
            color: var(--text-dim); text-transform: uppercase;
        }
        .scroll-line {
            width: 1px; height: 48px;
            background: linear-gradient(to bottom, rgba(0,255,136,0.6), transparent);
            animation: scrollAnim 2s ease infinite;
        }
        @keyframes scrollAnim {
            0% { transform: scaleY(0); transform-origin: top; }
            50% { transform: scaleY(1); transform-origin: top; }
            51% { transform: scaleY(1); transform-origin: bottom; }
            100% { transform: scaleY(0); transform-origin: bottom; }
        }

        /* ── DIVIDER ── */
        .section-divider {
            position: relative; z-index: 1;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(0,255,136,0.2), transparent);
            margin: 0 48px;
        }

        /* ── SECCIÓN: EVENTOS ── */
        #eventos {
            padding: 100px 24px;
            flex-direction: column;
        }

        .section-header {
            text-align: center;
            margin-bottom: 72px;
        }
        .section-tag {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.35em;
            color: var(--green); text-transform: uppercase;
            display: block; margin-bottom: 16px;
        }
        .section-title {
            font-family: 'Orbitron', monospace;
            font-size: clamp(1.8rem, 4vw, 3.5rem);
            font-weight: 900;
            color: #fff;
            line-height: 1.1;
        }
        .section-subtitle {
            font-family: 'Crimson Pro', serif;
            font-size: 1.2rem; font-style: italic;
            color: var(--text-dim);
            margin-top: 12px;
        }

        /* Timeline de eventos */
        .timeline {
            width: 100%; max-width: 900px;
            position: relative;
        }
        .timeline::before {
            content: '';
            position: absolute;
            left: 50%; top: 0; bottom: 0;
            width: 1px;
            background: linear-gradient(to bottom, transparent, rgba(0,255,136,0.3) 10%, rgba(0,255,136,0.3) 90%, transparent);
            transform: translateX(-50%);
        }

        .timeline-item {
            display: grid;
            grid-template-columns: 1fr 48px 1fr;
            align-items: start;
            margin-bottom: 64px;
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.7s ease, transform 0.7s ease;
        }
        .timeline-item.visible { opacity: 1; transform: translateY(0); }

        .timeline-item:nth-child(odd) .event-card { grid-column: 1; text-align: right; }
        .timeline-item:nth-child(odd) .event-empty { grid-column: 3; }
        .timeline-item:nth-child(even) .event-card { grid-column: 3; text-align: left; }
        .timeline-item:nth-child(even) .event-empty { grid-column: 1; }

        .timeline-dot {
            grid-column: 2;
            display: flex; align-items: center; justify-content: center;
            padding-top: 4px;
        }
        .dot-inner {
            width: 12px; height: 12px;
            border-radius: 50%;
            background: var(--green);
            box-shadow: 0 0 16px var(--green);
            flex-shrink: 0;
        }

        .event-card {
            padding: 0 32px;
        }
        .event-date {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.25em;
            color: var(--green); text-transform: uppercase;
            margin-bottom: 8px;
        }
        .event-name {
            font-family: 'Orbitron', monospace;
            font-size: 1.1rem; font-weight: 700;
            color: #fff; margin-bottom: 6px;
        }
        .event-place {
            font-family: 'Crimson Pro', serif;
            font-size: 1rem; font-style: italic;
            color: var(--text-dim);
            margin-bottom: 10px;
        }
        .event-desc {
            font-size: 0.95rem; font-weight: 300;
            color: rgba(255,255,255,0.5);
            line-height: 1.6;
        }
        .event-badge {
            display: inline-block;
            font-family: 'Orbitron', monospace;
            font-size: 0.5rem; letter-spacing: 0.15em;
            padding: 4px 10px; border-radius: 2px;
            margin-top: 10px;
        }
        .badge-ceremony { background: rgba(255,215,0,0.1); color: var(--gold); border: 1px solid rgba(255,215,0,0.2); }
        .badge-party { background: rgba(0,136,255,0.1); color: var(--blue); border: 1px solid rgba(0,136,255,0.2); }
        .badge-brunch { background: rgba(255,0,204,0.1); color: var(--pink); border: 1px solid rgba(255,0,204,0.2); }

        /* ── SECCIÓN: LUGAR ── */
        #lugar {
            padding: 100px 24px;
            flex-direction: column;
            background: radial-gradient(ellipse at center, rgba(0,136,255,0.04) 0%, transparent 70%);
        }

        .lugar-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2px;
            max-width: 900px;
            width: 100%;
            border: 1px solid var(--border);
            border-radius: 4px;
            overflow: hidden;
            margin-top: 48px;
        }
        .lugar-grid + .lugar-grid { margin-top: 24px; }
        .lugar-info {
            background: var(--surface);
            padding: 48px;
            display: flex; flex-direction: column; justify-content: center;
        }
        .lugar-coords {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.2em;
            color: var(--green); margin-bottom: 20px;
        }
        .lugar-name {
            font-family: 'Orbitron', monospace;
            font-size: 1.6rem; font-weight: 900;
            color: #fff; margin-bottom: 8px; line-height: 1.2;
        }
        .lugar-city {
            font-family: 'Crimson Pro', serif;
            font-style: italic; font-size: 1.2rem;
            color: var(--text-dim); margin-bottom: 24px;
        }
        .lugar-desc {
            font-size: 1rem; font-weight: 300;
            color: rgba(255,255,255,0.5);
            line-height: 1.7; margin-bottom: 28px;
        }
        .map-btn {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.2em; text-transform: uppercase;
            color: var(--green);
            border: 1px solid rgba(0,255,136,0.3);
            padding: 12px 20px; border-radius: 3px;
            text-decoration: none; display: inline-block;
            transition: background 0.2s, color 0.2s;
        }
        .map-btn:hover {
            background: rgba(0,255,136,0.08);
        }
        .lugar-visual {
            background: rgba(0,0,0,0.5);
            position: relative; overflow: hidden;
            min-height: 360px;
            display: flex; align-items: center; justify-content: center;
        }
        .lugar-visual-inner {
            text-align: center;
            padding: 32px;
        }
        .plaza-icon {
            font-size: 4rem; margin-bottom: 16px; display: block;
            filter: drop-shadow(0 0 20px rgba(255,215,0,0.4));
        }
        .plaza-label {
            font-family: 'Orbitron', monospace;
            font-size: 0.65rem; letter-spacing: 0.3em;
            color: var(--text-dim);
        }
        /* Grid decorativo de fondo */
        .lugar-visual::before {
            content: '';
            position: absolute; inset: 0;
            background-image:
                linear-gradient(rgba(0,136,255,0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0,136,255,0.05) 1px, transparent 1px);
            background-size: 40px 40px;
        }

        /* ── SECCIÓN: JUEGO ── */
        #juego {
            padding: 100px 24px;
            flex-direction: column;
            background: radial-gradient(ellipse at center, rgba(255,0,204,0.04) 0%, transparent 70%);
        }

        .game-card {
            max-width: 700px; width: 100%;
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: 4px; overflow: hidden;
            text-align: center;
            margin-top: 48px;
            position: relative;
        }
        .game-card-accent {
            height: 3px;
            background: linear-gradient(90deg, var(--pink), var(--blue), var(--green));
        }
        .game-card-body { padding: 56px 48px; }
        .game-icon { font-size: 3.5rem; margin-bottom: 20px; display: block; }
        .game-title {
            font-family: 'Orbitron', monospace;
            font-size: 1.5rem; font-weight: 900;
            color: #fff; margin-bottom: 12px;
        }
        .game-desc {
            font-size: 1.1rem; font-weight: 300; font-style: italic;
            color: var(--text-dim); line-height: 1.6;
            margin-bottom: 32px;
        }
        .game-coming-soon {
            display: inline-flex; align-items: center; gap: 8px;
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.2em;
            color: var(--pink);
            background: rgba(255,0,204,0.08);
            border: 1px solid rgba(255,0,204,0.2);
            padding: 10px 20px; border-radius: 3px;
        }
        .coming-dot {
            width: 6px; height: 6px; border-radius: 50%;
            background: var(--pink);
            animation: pulse 2s infinite;
        }

        /* ── SECCIÓN: ACCESO ── */
        #acceso {
            padding: 100px 24px;
            flex-direction: column;
            min-height: 60vh;
        }

        .acceso-inner {
            text-align: center; max-width: 600px;
        }
        .acceso-inner .section-title { margin-bottom: 16px; }
        .acceso-inner p {
            font-size: 1.15rem; font-style: italic; font-weight: 300;
            color: var(--text-dim); margin-bottom: 40px; line-height: 1.7;
        }
        .acceso-cta {
            font-family: 'Orbitron', monospace;
            font-size: 0.85rem; font-weight: 700;
            letter-spacing: 0.2em; text-transform: uppercase;
            color: #000; background: var(--green);
            padding: 20px 48px; border-radius: 3px;
            text-decoration: none; display: inline-block;
            box-shadow: 0 0 40px rgba(0,255,136,0.3);
            transition: background 0.2s, box-shadow 0.2s, transform 0.15s;
        }
        .acceso-cta:hover {
            background: #33ffaa;
            box-shadow: 0 0 60px rgba(0,255,136,0.5);
            transform: translateY(-3px);
        }

        /* ── FOOTER ── */
        footer {
            position: relative; z-index: 1;
            border-top: 1px solid var(--border);
            padding: 32px 48px;
            display: flex; justify-content: space-between; align-items: center;
        }
        .footer-brand {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.25em;
            color: var(--text-dim);
        }
        .footer-heart {
            font-size: 0.9rem;
            color: var(--green);
        }
        .footer-date {
            font-family: 'Orbitron', monospace;
            font-size: 0.6rem; letter-spacing: 0.2em;
            color: var(--text-dim);
        }

        /* ── ANIMACIONES ── */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(24px); }
            to   { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to   { opacity: 1; }
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.3; }
        }

        /* ── RESPONSIVE ── */
        @media (max-width: 768px) {
            nav { padding: 16px 24px; }
            .nav-links { display: none; }
            .timeline::before { left: 24px; }
            .timeline-item {
                grid-template-columns: 48px 1fr;
            }
            .timeline-item:nth-child(odd) .event-card,
            .timeline-item:nth-child(even) .event-card {
                grid-column: 2; text-align: left;
            }
            .timeline-item:nth-child(odd) .event-empty,
            .timeline-item:nth-child(even) .event-empty { display: none; }
            .timeline-dot { grid-column: 1; }
            .event-card { padding: 0 0 0 16px; }
            .lugar-grid { grid-template-columns: 1fr; }
            .countdown { gap: 16px; }
            footer { flex-direction: column; gap: 12px; text-align: center; }
        }
    </style>
</head>
<body>

    <!-- Cursor personalizado -->
    <div class="cursor" id="cursor"></div>
    <div class="cursor-ring" id="cursorRing"></div>

    <!-- Fondo de estrellas -->
    <canvas id="stars-canvas"></canvas>

    <!-- NAV -->
    <nav id="navbar">
        <a href="/" class="nav-brand">✦ Lee atentamente ✦ además haz click sobre una estrella para experiencia auditia</a>
        <ul class="nav-links">
            <li><a href="#eventos">Eventos</a></li>
            <li><a href="#lugar">Lugar</a></li>
            <li><a href="#juego">Juego</a></li>
        </ul>
        <a href="/intro" class="nav-cta">Acceder →</a>
    </nav>

    <!-- ══ HERO ══ -->
    <section id="hero">
        <p class="hero-eyebrow">✦ Una boda entre las estrellas · 2026 / 2027</p>

        <h1 class="hero-title">
            Boda<br>
            <span class="accent">Camilo & Víctor</span>
        </h1>

        <p class="hero-names">os invitamos a compartir este viaje</p>

        <!-- Countdown -->
        <div class="countdown" id="countdown">
            <div class="count-item">
                <span class="count-number" id="cd-days">--</span>
                <span class="count-label">Días</span>
            </div>
            <span class="count-sep">:</span>
            <div class="count-item">
                <span class="count-number" id="cd-hours">--</span>
                <span class="count-label">Horas</span>
            </div>
            <span class="count-sep">:</span>
            <div class="count-item">
                <span class="count-number" id="cd-mins">--</span>
                <span class="count-label">Minutos</span>
            </div>
            <span class="count-sep">:</span>
            <div class="count-item">
                <span class="count-number" id="cd-secs">--</span>
                <span class="count-label">Segundos</span>
            </div>
        </div>

        <div class="hero-cta-group">
            <a href="/intro" class="btn-primary">Entrar a la boda →</a>
            <a href="#eventos" class="btn-secondary">Ver programa</a>
        </div>

        <div class="scroll-hint">
            <span>Scroll</span>
            <div class="scroll-line"></div>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- ══ EVENTOS ══ -->
    <section id="eventos">
        <div class="section-header">
            <span class="section-tag">✦ Programa</span>
            <h2 class="section-title">Dos años,<br>una historia</h2>
            <p class="section-subtitle">La celebración se divide en dos momentos únicos</p>
        </div>

        <div class="timeline">

            <!-- Evento 1: Ceremonia 2026 -->
            <div class="timeline-item">
                <div class="event-card">
                    <div class="event-date">Viernes 22 · Mayo · 2026</div>
                    <div class="event-name">La Ceremonia</div>
                    <div class="event-place">Plaza Mayor, Madrid</div>
                    <div class="event-desc">El momento en que todo empieza. Nos damos el sí ante quienes más queremos, en el corazón de Madrid.</div>
                    <span class="event-badge badge-ceremony">⭐ Ceremonia Civil</span>
                </div>
                <div class="timeline-dot"><div class="dot-inner"></div></div>
                <div class="event-empty"></div>
            </div>

            <!-- Evento 2: Viernes fiesta -->
            <div class="timeline-item">
                <div class="event-empty"></div>
                <div class="timeline-dot"><div class="dot-inner"></div></div>
                <div class="event-card">
                    <div class="event-date">Viernes 21 · Mayo · 2027</div>
                    <div class="event-name">La Escapada</div>
                    <div class="event-place">Llegada y cena de bienvenida</div>
                    <div class="event-desc">El viaje comienza la noche del viernes. Cena íntima, reencuentros y el inicio de un fin de semana inolvidable.</div>
                    <span class="event-badge badge-party">🚀 Noche 1</span>
                </div>
            </div>

            <!-- Evento 3: Sábado -->
            <div class="timeline-item">
                <div class="event-card">
                    <div class="event-date">Sábado 22 · Mayo · 2027</div>
                    <div class="event-name">La Gran Fiesta</div>
                    <div class="event-place">Ceremonia · Cóctel · Banquete · Fiesta</div>
                    <div class="event-desc">El día grande. Renovamos votos, brindamos, comemos, bailamos. La noche que recordaréis toda la vida.</div>
                    <span class="event-badge badge-party">🎉 Noche 2</span>
                </div>
                <div class="timeline-dot"><div class="dot-inner"></div></div>
                <div class="event-empty"></div>
            </div>

            <!-- Evento 4: Domingo -->
            <div class="timeline-item">
                <div class="event-empty"></div>
                <div class="timeline-dot"><div class="dot-inner"></div></div>
                <div class="event-card">
                    <div class="event-date">Domingo 23 · Mayo · 2027</div>
                    <div class="event-name">La Despedida</div>
                    <div class="event-place">Última mañana juntos</div>
                    <div class="event-desc">Una mañana tranquila para alargar el momento. Sin prisas, con recuerdos frescos y el calor del grupo antes de volver a casa.</div>
                    <span class="event-badge badge-brunch">☀️ Mañana de despedida</span>
                </div>
            </div>

        </div>
    </section>

    <div class="section-divider"></div>

    <!-- ══ LUGAR ══ -->
    <section id="lugar">
        <div class="section-header">
            <span class="section-tag">✦ Los escenarios</span>
            <h2 class="section-title">Dos lugares,<br>una historia</h2>
            <p class="section-subtitle">Del corazón de Madrid a la naturaleza más pura de Guadalajara</p>
        </div>

        <!-- LUGAR 1: Plaza Mayor 2026 -->
        <div class="lugar-grid" style="margin-bottom: 2px;">
            <div class="lugar-info">
                <div class="lugar-coords" style="color: var(--gold);">2026 · FASE I</div>
                <div class="lugar-name">Plaza Mayor</div>
                <div class="lugar-city">Madrid, España</div>
                <p class="lugar-desc">Uno de los espacios más emblemáticos de España. Construida en el siglo XVII, la Plaza Mayor ha sido testigo de coronaciones y celebraciones históricas. El 22 de mayo de 2026 también será testigo de nuestra ceremonia.</p>
                <a href="https://maps.google.com/?q=Plaza+Mayor+Madrid" target="_blank" class="map-btn" style="border-color: rgba(255,215,0,0.3); color: var(--gold);">Ver en Google Maps →</a>
            </div>
            <div class="lugar-visual">
                <div style="width:100%; height:100%; position:absolute; inset:0; background:#0a0a14; display:flex; align-items:center; justify-content:center; flex-direction:column; gap:12px; padding:40px;">
             <img src="/images/Plazamayor.jpg" alt="Plaza Mayor Madrid" style="max-width:75%; max-height:200px; object-fit:contain; filter:invert(1) brightness(0.6) sepia(1) saturate(3) hue-rotate(100deg);">
             <div class="plaza-label">PLAZA MAYOR · MADRID · 🌍</div>
        </div>
    </div>
            </div>
        </div>

        <!-- LUGAR 2: Aldea Tejera Negra 2027 -->
        <div class="lugar-grid" style="margin-top: 2px;">
            <div class="lugar-visual" style="order: -1;">
                <div class="lugar-visual-inner">
                    <span class="plaza-icon" style="filter: drop-shadow(0 0 20px rgba(0,255,136,0.4));">🌲</span>
                    <div class="plaza-label">TEJERA NEGRA · GUADALAJARA · ARQUITECTURA NEGRA</div>
                </div>
            </div>
            <div class="lugar-info">
                <div class="lugar-coords" style="color: var(--green);">2027 · FASE II</div>
                <div class="lugar-name">Aldea Tejera Negra</div>
                <div class="lugar-city">Campillo de Ranas, Guadalajara</div>
                <p class="lugar-desc">Un lugar único en el mundo. La arquitectura negra de pizarra de Campillo de Ranas, rodeada de hayedos centenarios y silencio absoluto. Tejera Negra es naturaleza pura, encanto medieval y magia serrana. El escenario perfecto para el fin de semana más especial.</p>
                <div style="display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 16px;">
                    <span class="event-badge badge-party" style="font-size: 0.55rem;">🌿 Naturaleza</span>
                    <span class="event-badge badge-ceremony" style="font-size: 0.55rem;">🪨 Arquitectura Negra</span>
                    <span class="event-badge badge-brunch" style="font-size: 0.55rem;">🏔️ Sierra Norte</span>
                </div>
                <a href="https://maps.google.com/?q=Campillo+de+Ranas+Guadalajara" target="_blank" class="map-btn">Ver en Google Maps →</a>
            </div>
        </div>
    </section>

    <div class="section-divider"></div>

    <!-- ══ JUEGO ══ -->
    <section id="juego">
        <div class="section-header">
            <span class="section-tag">✦ Sorpresa</span>
            <h2 class="section-title">El Juego</h2>
            <p class="section-subtitle">Una pequeña aventura con premio incluido</p>
        </div>

        <div class="game-card">
            <div class="game-card-accent"></div>
            <div class="game-card-body">
            <span class="game-icon">🎮</span>
        <h3 class="game-title">¿Cuánto nos conoces?</h3>
        <p class="game-desc">Demuestra cuánto sabes sobre Víctor y Camilo. 10 preguntas, 3 vidas y un ranking entre todos los invitados.</p>

    <!-- TOP 3 RANKING -->
        <div id="landing-ranking" style="margin: 24px 0; display:flex; flex-direction:column; gap:8px;">
        <div style="font-family:'Orbitron',monospace; font-size:0.55rem; letter-spacing:0.25em; color:rgba(255,255,255,0.25); margin-bottom:4px;">🏆 RANKING ACTUAL</div>
        <div id="landing-top3" style="display:flex; flex-direction:column; gap:6px;">
        <div style="color:rgba(255,255,255,0.2); font-size:0.8rem; font-style:italic;">Cargando ranking...</div>
        </div>
    </div>
    <a href="/game" class="btn-primary" style="display:inline-block; margin-top:8px;">🎮 Jugar ahora →</a>
</div>

<script>
fetch('/game/ranking').then(r=>r.json()).then(ranking => {
    const el = document.getElementById('landing-top3');
    if (!ranking.length) {
        el.innerHTML = '<div style="color:rgba(255,255,255,0.2); font-size:0.8rem; font-style:italic;">Sé el primero en jugar 🚀</div>';
        return;
    }
    const medals = ['🥇','🥈','🥉'];
    el.innerHTML = ranking.slice(0,3).map((r,i) => `
        <div style="display:flex; align-items:center; gap:12px; background:rgba(255,255,255,0.03); border:1px solid rgba(255,255,255,0.06); border-radius:4px; padding:10px 14px;">
            <span style="font-size:1.1rem;">${medals[i]}</span>
            <span style="flex:1; font-family:'Orbitron',monospace; font-size:0.7rem; color:#fff;">${r.player_name}</span>
            <span style="font-family:'Orbitron',monospace; font-size:0.7rem; color:#00ff88; font-weight:700;">${parseInt(r.score).toLocaleString()} pts</span>
        </div>
    `).join('');
}).catch(() => {});
</script>
    </section>

    <div class="section-divider"></div>

    <!-- ══ ACCESO ══ -->
    <section id="acceso">
        <div class="acceso-inner">
            <span class="section-tag">✦ Zona de invitados</span>
            <h2 class="section-title">¿Tienes tu código?</h2>
            <p>Cada grupo de invitados ha recibido un código único. Úsalo para confirmar tu asistencia, gestionar el alojamiento y acceder al juego.</p>
            <a href="/intro" class="acceso-cta">Aterrizar → Entrar</a>
        </div>
    </section>

    <!-- ══ FOOTER ══ -->
    <footer>
        <span class="footer-brand">✦ Diseño web: Camilo Francisco</span>
        <span class="footer-heart">♥</span>
        <span class="footer-date">SISTEMA SOLAR - VIA LACTEA</span>
    </footer>

    <script>
        // ── CURSOR ──
        const cursor = document.getElementById('cursor');
        const ring = document.getElementById('cursorRing');
        let mx = 0, my = 0, rx = 0, ry = 0;
        document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
        function animCursor() {
            cursor.style.left = (mx - 4) + 'px';
            cursor.style.top = (my - 4) + 'px';
            rx += (mx - rx) * 0.12;
            ry += (my - ry) * 0.12;
            ring.style.left = (rx - 16) + 'px';
            ring.style.top = (ry - 16) + 'px';
            requestAnimationFrame(animCursor);
        }
        animCursor();
        document.querySelectorAll('a, button').forEach(el => {
            el.addEventListener('mouseenter', () => {
                cursor.style.transform = 'scale(2)';
                ring.style.width = '48px';
                ring.style.height = '48px';
            });
            el.addEventListener('mouseleave', () => {
                cursor.style.transform = 'scale(1)';
                ring.style.width = '32px';
                ring.style.height = '32px';
            });
        });

        // ── ESTRELLAS ──
        const canvas = document.getElementById('stars-canvas');
        const ctx = canvas.getContext('2d');
        let stars = [];
        function initStars() {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
            stars = Array.from({ length: 200 }, () => ({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                r: Math.random() * 1.2,
                o: Math.random() * 0.7 + 0.1,
                speed: Math.random() * 0.3 + 0.05
            }));
        }
        function drawStars() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            stars.forEach(s => {
                ctx.beginPath();
                ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(255,255,255,${s.o})`;
                ctx.fill();
                // Parallax suave con scroll
                s.y += s.speed;
                if (s.y > canvas.height) { s.y = 0; s.x = Math.random() * canvas.width; }
            });
            requestAnimationFrame(drawStars);
        }
        initStars();
        drawStars();
        window.addEventListener('resize', initStars);

        // ── NAV SCROLL ──
        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', () => {
            navbar.classList.toggle('scrolled', window.scrollY > 60);
        });

        // ── COUNTDOWN ──
        function updateCountdown() {
            const target = new Date('2027-05-22T12:00:00');
            const now = new Date();
            const diff = target - now;
            if (diff <= 0) return;
            const d = Math.floor(diff / 86400000);
            const h = Math.floor((diff % 86400000) / 3600000);
            const m = Math.floor((diff % 3600000) / 60000);
            const s = Math.floor((diff % 60000) / 1000);
            document.getElementById('cd-days').textContent = String(d).padStart(3, '0');
            document.getElementById('cd-hours').textContent = String(h).padStart(2, '0');
            document.getElementById('cd-mins').textContent = String(m).padStart(2, '0');
            document.getElementById('cd-secs').textContent = String(s).padStart(2, '0');
        }
        updateCountdown();
        setInterval(updateCountdown, 1000);

        // ── SCROLL REVEAL (timeline) ──
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((e, i) => {
                if (e.isIntersecting) {
                    setTimeout(() => e.target.classList.add('visible'), i * 150);
                }
            });
        }, { threshold: 0.2 });
        document.querySelectorAll('.timeline-item').forEach(el => observer.observe(el));
    </script>

    <audio id="enyaAudio" loop>
    <source src="/audio/enya.mp3" type="audio/mpeg">
</audio>
<script>
    //Audio enya caribbean blue
const enyaAudio = document.getElementById('enyaAudio');
enyaAudio.volume = 0.50;
document.addEventListener('click', () => {
    enyaAudio.play().catch(() => {});
}, { once: true });
document.addEventListener('touchstart', () => {
    enyaAudio.play().catch(() => {});
}, { once: true });
</script>

</body>
</html>
