<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Admin · Nuestra Boda</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body { min-height: 100vh; background: #000; display: flex; align-items: center; justify-content: center; font-family: 'Inter', sans-serif; padding: 24px;
            background-image: radial-gradient(ellipse at 30% 50%, rgba(255,0,204,0.04) 0%, transparent 60%), radial-gradient(ellipse at 70% 30%, rgba(0,136,255,0.04) 0%, transparent 60%);
        }
        .card-wrapper { position: relative; width: 100%; max-width: 420px; }
        .corner { position: absolute; width: 12px; height: 12px; border-color: rgba(255,0,204,0.4); border-style: solid; }
        .corner-tl { top: -1px; left: -1px; border-width: 2px 0 0 2px; }
        .corner-tr { top: -1px; right: -1px; border-width: 2px 2px 0 0; }
        .corner-bl { bottom: -1px; left: -1px; border-width: 0 0 2px 2px; }
        .corner-br { bottom: -1px; right: -1px; border-width: 0 2px 2px 0; }
        .card { background: rgba(8,8,20,0.95); border: 1px solid rgba(255,255,255,0.07); border-radius: 4px; overflow: hidden; box-shadow: 0 30px 80px rgba(0,0,0,0.8); }
        .card-accent { height: 3px; background: linear-gradient(90deg, #ff00cc, #0088ff, #00ff88); }
        .card-body { padding: 40px; }
        .brand { font-family: 'Orbitron', monospace; font-size: 0.6rem; font-weight: 700; letter-spacing: 0.3em; text-transform: uppercase; color: #ff00cc; margin-bottom: 20px; }
        h1 { font-family: 'Orbitron', monospace; font-size: 1.5rem; font-weight: 900; color: #fff; margin-bottom: 6px; }
        .subtitle { font-size: 0.85rem; color: rgba(255,255,255,0.35); margin-bottom: 32px; }
        .field { margin-bottom: 14px; }
        label { display: block; font-family: 'Orbitron', monospace; font-size: 0.55rem; letter-spacing: 0.2em; color: rgba(255,255,255,0.35); text-transform: uppercase; margin-bottom: 6px; }
        input { width: 100%; background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.09); border-radius: 3px; padding: 12px 14px; color: #fff; font-family: 'Inter', sans-serif; font-size: 0.9rem; outline: none; transition: border-color 0.2s; }
        input:focus { border-color: rgba(255,0,204,0.4); box-shadow: 0 0 0 3px rgba(255,0,204,0.06); }
        button { width: 100%; margin-top: 20px; padding: 14px; background: #ff00cc; color: #fff; font-family: 'Orbitron', monospace; font-size: 0.75rem; font-weight: 700; letter-spacing: 0.2em; text-transform: uppercase; border: none; border-radius: 3px; cursor: pointer; transition: background 0.2s, box-shadow 0.2s; box-shadow: 0 0 20px rgba(255,0,204,0.2); }
        button:hover { background: #ff33d6; box-shadow: 0 0 30px rgba(255,0,204,0.4); }
        .error { background: rgba(255,60,60,0.08); border: 1px solid rgba(255,60,60,0.25); border-left: 3px solid #ff3c3c; border-radius: 3px; padding: 10px 14px; margin-bottom: 20px; color: #ff7070; font-size: 0.82rem; }
        .back { display: block; text-align: center; margin-top: 20px; font-family: 'Orbitron', monospace; font-size: 0.55rem; letter-spacing: 0.15em; color: rgba(255,255,255,0.2); text-decoration: none; }
        .back:hover { color: rgba(255,255,255,0.4); }
    </style>
</head>
<body>
    <div class="card-wrapper">
        <div class="corner corner-tl"></div>
        <div class="corner corner-tr"></div>
        <div class="corner corner-bl"></div>
        <div class="corner corner-br"></div>
        <div class="card">
            <div class="card-accent"></div>
            <div class="card-body">
                <div class="brand">✦ Acceso restringido</div>
                <h1>Panel Admin</h1>
                <p class="subtitle">Solo para los novios</p>
                <% if (error) { %>
                <div class="error">⚠ <%= error %></div>
                <% } %>
                <form action="/admin/login" method="POST">
                    <div class="field">
                        <label>Usuario</label>
                        <input type="text" name="username" required autocomplete="off">
                    </div>
                    <div class="field">
                        <label>Contraseña</label>
                        <input type="password" name="password" required>
                    </div>
                    <button type="submit">Acceder →</button>
                </form>
                <a href="/" class="back">← Volver a la web</a>
            </div>
        </div>
    </div>
</body>
</html>
